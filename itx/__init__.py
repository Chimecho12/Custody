"""itx — 독립 제3자 기반 AI 서비스 경로 검증·정책 집행 플랫폼 (참조 구현).

사용자(U) ↔ 중개자(R) ↔ 모델 운영자(M) 경로에서 각 당사자가 서명한 진술을
독립 제3자(T)가 등록·대조하고, 사용자 측 집행 모듈이 그 결과와 자체 검증으로
변조된 응답을 업무에 쓰기 전에 거부하는 흐름을 시뮬레이션과 함께 구현한다.

이 패키지는 교육·연구용 참조 구현이다. COSE/SCITT/AIR 표준 적합성을 주장하지
않으며, 자체 JSON 프로파일(`itx-profile/0.1`)을 사용한다. 자세한 한계는
docs/limits.md 를 보라.
"""

__version__ = "0.1.0"

#: 대조 엔진(검사기) 버전. 판정 진술에 항상 기록된다.
CHECKER_VERSION = "itx-reconcile/0.1.0"

#: 진술 프로파일 버전. 모든 진술 페이로드의 `profile` 필드 접두어.
PROFILE_VERSION = "itx-profile/0.1"

#: 모든 판정·영수증은 평가 모드다. 프로덕션 신뢰 결정에 쓰면 안 된다.
DEPLOYMENT_MODE = "evaluation"
