[package]
name = "itx-desktop"
version = "0.3.0"
description = "itx AI route integrity evaluation desktop"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde_json = "1"
tokio = { version = "1", features = ["sync", "time"] }
